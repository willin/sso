{
  "extends": ["//"],
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "deploy": {
      "outputs": ["src/**"]
    },
    "dev": {
      "dependsOn": [
        "@hono-dev/auth-afdian#build",
        "@hono-dev/auth-alipay#build",
        "@hono-dev/auth-github#build",
        "@hono-dev/powered-by#build"
      ]
    },
    "test": {
      "outputs": [".coverage/**"]
    }
  }
}
